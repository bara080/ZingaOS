# runs/

Append-only logs of what agents actually did. Dated files, never edited.

This is the audit trail. When an automated run sends 40 emails at 3am, this is
the only place that says what went out and to whom.

JSON and .log files here are gitignored — they are local history, not shared
state.
