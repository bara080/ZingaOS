{
  "name": "nightly-outreach",
  "active": false,
  "settings": { "executionOrder": "v1", "saveDataErrorExecution": "all" },
  "nodes": [
    {
      "parameters": {
        "rule": { "interval": [ { "field": "cronExpression", "expression": "0 8 * * 1" } ] }
      },
      "id": "trigger-cron",
      "name": "Mon 08:00",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [ 240, 300 ]
    },
    {
      "parameters": {
        "command": "cd /Users/bara080/bara/zinga-os && claude -p \"/outreach-run 20 brooklyn barber-beauty\" --allowedTools \"Read,Write,Edit,Bash(python3 tools/outreach_mailer.py)\" --permission-mode acceptEdits --output-format json --max-turns 25 --max-budget-usd 5.00 >> \"runs/$(date +%F)-outreach.json\""
      },
      "id": "exec-outreach",
      "name": "Execute /outreach-run (DRAFTS ONLY)",
      "type": "n8n-nodes-base.executeCommand",
      "typeVersion": 1,
      "position": [ 520, 300 ]
    },
    {
      "parameters": {
        "text": "=nightly-outreach done. turns={{$json[\"num_turns\"]}} — drafts staged in runs/, NOT sent. Review + approve before --send.\nGuard: if num_turns==0 or result starts 'Unknown command', the run is dead (see n8n/README.md)."
      },
      "id": "notify-slack",
      "name": "Log + notify Bara",
      "type": "n8n-nodes-base.slack",
      "typeVersion": 2.2,
      "position": [ 800, 300 ],
      "credentials": { "slackApi": { "name": "Zinga Slack" } }
    }
  ],
  "connections": {
    "Mon 08:00": { "main": [ [ { "node": "Execute /outreach-run (DRAFTS ONLY)", "type": "main", "index": 0 } ] ] },
    "Execute /outreach-run (DRAFTS ONLY)": { "main": [ [ { "node": "Log + notify Bara", "type": "main", "index": 0 } ] ] }
  }
}
